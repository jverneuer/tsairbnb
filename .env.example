# AWS account/region for CDK bootstrap + deploy. Copy to .env and fill in.
CDK_DEFAULT_ACCOUNT=
CDK_DEFAULT_REGION=us-east-1
# Target Lambda platform: linux/amd64 (cheaper) or linux/arm64 (Graviton).
TARGETPLATFORM=linux/amd64
